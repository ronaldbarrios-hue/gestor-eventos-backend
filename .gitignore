node_modules/
.env
.env.local
coverage/
dist/
.DS_Store
.claude/

# Volcado de datos de la base — datos personales de producción (correos,
# teléfonos, tokens OAuth, secretos de pasarela). NO va a Git. Se regenera con
# db/esquema/generar-datos.mjs. Ver db/esquema/README.md y la nota legal LG-001.
db/esquema/03_datos.sql
db/esquema/_datos_json/
datos-crudos.sql

# Igual: hashes de contraseña de cuentas reales. Se regenera con
# db/migraciones/generar-usuarios.mjs. Se pega en gestek_auth y no en Git.
db/migraciones/salida-usuarios.sql

# Copia local de archivos reales de usuarios (fotos, documentos) descargados
# de Supabase Storage para la migración Camino A. Datos personales reales,
# NUNCA va a Git.
.archivos/
